# Recording Tabs 224x44 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 顶部已打开的所有 tab（包含 Home tab 和任意站点 tab）统一尺寸为宽 224px、高 44px。

**Architecture:** 直接在 `RecordingTabStrip` 的 tab button 上固定宽高（Tailwind class），不再使用不同 tab 的 min/max 宽度逻辑；保持既有 hover/active 背景与 close/recording indicator 行为不变。

**Tech Stack:** React + TypeScript + TailwindCSS + Vitest + Testing Library

---

## Files To Change

- Modify: [RecordingTabStrip.tsx](file:///c:/Users/fab/yuki-studio-repo/pm-workspace/.product_knowledge/RecordFab/recordfab-prototype/src/components/RecordingTabStrip.tsx)
- Modify: [RecordingTabStrip.test.tsx](file:///c:/Users/fab/yuki-studio-repo/pm-workspace/.product_knowledge/RecordFab/recordfab-prototype/src/components/RecordingTabStrip.test.tsx)

---

### Task 1: 固定 tab 尺寸为 224×44

**Files:**
- Modify: `c:/Users/fab/yuki-studio-repo/pm-workspace/.product_knowledge/RecordFab/recordfab-prototype/src/components/RecordingTabStrip.tsx`

- [ ] **Step 1: 写失败测试（tab 宽高 class）**

在现有 `RecordingTabStrip.test.tsx` 增加用例：

```tsx
it('uses fixed tab size 224x44 for all tabs', () => {
  render(
    <RecordingTabStrip
      tabs={[
        { id: 'home', label: 'Home', iconHome: true },
        { id: 't1', label: 'Netflix', iconSrc: '/figma/tab_icon_netflix.svg' },
      ]}
      activeTabId="home"
      onSelect={() => {}}
    />
  )

  const home = screen.getByRole('button', { name: 'Home' })
  const netflix = screen.getByRole('button', { name: /netflix/i })

  expect(home.className.includes('h-11')).toBe(true)
  expect(netflix.className.includes('h-11')).toBe(true)
  expect(home.className.includes('w-[224px]')).toBe(true)
  expect(netflix.className.includes('w-[224px]')).toBe(true)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run (PowerShell, in `c:/Users/fab/yuki-studio-repo/pm-workspace/.product_knowledge/RecordFab/recordfab-prototype`):

```bash
npm test
```

Expected: FAIL（非 Home tab 仍是 min/max 宽度，且 class 不包含 `w-[224px]`）

- [ ] **Step 3: 最小实现：移除变宽逻辑，tab button 固定 `w-[224px] h-11`**

在 `RecordingTabStrip.tsx`：
- 移除 `sizeClass`（home 与其它 tab 的宽度分支）
- 将 tab button 的 `w-auto` / `${sizeClass}` 改为固定 `w-[224px]`
- 保持 `h-11` 不变（44px）

- [ ] **Step 4: 运行测试确认通过**

```bash
npm test
```

Expected: PASS

---

### Task 2: 回归校验（lint + build）

**Files:**
- None

- [ ] **Step 1: 运行 lint**

```bash
npm run lint
```

Expected: exit code 0

- [ ] **Step 2: 运行 build**

```bash
npm run build
```

Expected: exit code 0

